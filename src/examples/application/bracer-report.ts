/**
 * Flow
 * - [] Check If member name is store in bracer.json, If not it's mean error.
 * - [] If it's not error, we plus bracer point for the member.
 * - [] If member reach their bracer point to the target point then their rand will be updated.
 */

import * as bracerData from '../../data/bracer.json';

const memberList = ['Tita', 'Estelle'];
